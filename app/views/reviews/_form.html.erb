<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= label_tag 'rating_average','average'  %>
    <%= check_box_tag 'review[rating][]','average',checked2('average'),id: 'rating_average' %>
  </div>
  <div class="form-group">
    <%= label_tag 'rating_good','good'  %>
    <%= check_box_tag 'review[rating][]','good',checked2('good'),id: 'rating_good' %>
  </div>
  <div class="form-group">
    <%= label_tag 'rating_excellent','excellent'  %>
    <%= check_box_tag 'review[rating][]','excellent',checked2('excellent'),id: 'rating_excellent' %>
  </div>
    <div class="form-group">
    <%= f.label :reviews %><br>
    <%= f.text_field :body, class: "form-control" %>
  </div>
  <br>
  <div class="form-group">
    <%= f.submit "submmit", class: "btn btn-lg btn-primary" %>
  </div>

<% end %>
