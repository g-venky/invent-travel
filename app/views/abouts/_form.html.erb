<%= form_for @about do |f| %>
  <% if @about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% @about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 

  <div class="form-group">
    <%= f.label :about %><br>
    <%= f.text_field :write, class: "form-control" %>
  </div>
 
  <br>
  <div class="form-group">
    <%= f.submit "create about", class: "btn btn-lg btn-primary" %>
  </div>

<% end %>